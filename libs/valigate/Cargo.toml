[package]
name = "valigate"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0.17"
valigate_derive = { path = "../valigate_derive" }
chrono = { workspace = true, optional = true }

[features]
default = ["chrono"]
chrono = ["dep:chrono"]
