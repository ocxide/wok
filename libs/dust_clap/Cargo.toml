[package]
name = "dust_clap"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = "4.5.39"
dust = { path = "../../" }
dust_db = { path = "../dust_db" }
futures = { version = "0.3.31", no-default-features = true, features = ["std", "async-await"] }
take_mut = "0.2.2"
