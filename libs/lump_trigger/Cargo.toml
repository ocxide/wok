[package]
name = "lump_trigger"
version = "0.1.0"
edition = "2024"

[dependencies]
lump_core = { path = "../lump_core" }
futures = { version = "0.3.31", default-features = false, features = ["std", "async-await"] }
